import 'package:flutter/material.dart';

class Search extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      // appBar: new AppBar(
      //   title: new Text("中藥／藥方搜尋"),
      //   backgroundColor: Colors.green[900],
      // ),
      body: new Center(
        child: new Text("中藥／藥方搜尋"),
      ),
    );
  }
}
